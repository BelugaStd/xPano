Create a polished, production-realistic UI design mockup for xPano Workbench compact landscape view, a reconstruction app interface for monitoring panorama video, ordinary video, Metashape, COLMAP, and COLMAP export workflows.

Audience: 3D reconstruction users, Gaussian splatting creators, drone/action-camera operators, and technical artists.
Primary workflow: monitor extraction or alignment while keeping a compact preview stage and track/status controls visible.
Design system direction: Apple-inspired commercial workbench with layered rounded surfaces, restrained dark-neutral palette, clear hierarchy, subtle motion-ready components, and precise SVG iconography.
Design DNA blend: 3D / Interactive Site with supporting cues from Mobile-First App and Operational Dashboard.

Visual rules:
- Original design, not a copy of any existing brand or website.
- Realistic app interface that can be implemented in Qt Quick or React/CSS.
- Clear information hierarchy, readable UI labels, coherent component system.
- Use consistent typography, spacing, border radius, icon style, and color tokens.
- Avoid decorative gradient blobs, random bokeh/orbs, fake unreadable walls of text, and impossible UI geometry.
- Show the actual product interface, not a marketing poster.
- Do not use emoji as icons; use clean line icons.

Viewport: mobile landscape UI, horizontal composition, approximately 844x390.
Layout requirements:
- Use a landscape-native split view, not a squeezed desktop screenshot.
- Left compact rail shows tracks and per-track status.
- Right primary stage shows the latest extracted left/right fisheye previews side by side or a compact point cloud/camera viewport.
- Bottom strip shows stage progress and a short live log tail.
- Include compact toolbar controls for pause, stop, environment status, and output.
- Preserve the same design system as desktop while adapting density and interaction ergonomics.

Output: one single mobile landscape UI screen only. Do not include desktop views, portrait views, annotations, browser chrome, or multiple viewport mockups.
